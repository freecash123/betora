'use client';
import React from 'react';
export function DemoBanner() {
  return (<div className='demo-banner fixed top-0 left-0 right-0 z-50'>⚠️ <strong>BETORA DEMO MODE</strong> — Simulated funds only. Real-money betting is disabled.</div>);
}