'use client';
import React,{useState}from'react';import Link from'next/link';import{useAuth}from'@/app/layout';import{useRouter}from'next/navigation';import{Eye,EyeOff,Mail,Lock}from'lucide-react';import toast from'react-hot-toast';

export default function LoginPage(){
  const{login}=useAuth();const router=useRouter();const[email,setEmail]=useState('');const[password,setPassword]=useState('');const[show,setShow]=useState(false);const[loading,setLoading]=useState(false);const[error,setError]=useState('');
  const handleSubmit=async(e:React.FormEvent)=>{e.preventDefault();setLoading(true);setError('');try{await login(email,password);toast.success('Welcome back!');router.push('/')}catch(err:any){setError(err.message)}finally{setLoading(false)}};
  return <div className='min-h-screen bg-surface flex items-center justify-center px-4'><div className='w-full max-w-md'><div className='text-center mb-8'><Link href='/' className='inline-flex items-center gap-2 mb-4'><div className='w-10 h-10 bg-primary rounded-xl flex items-center justify-center font-bold text-white text-lg'>B</div><span className='text-2xl font-extrabold'>BET<span className='text-primary'>ORA</span></span></Link><h1 className='text-2xl font-bold'>Welcome Back</h1><p className='text-text-secondary text-sm mt-1'>Sign in to your account</p></div><form onSubmit={handleSubmit} className='bg-surface-light border border-surface-border rounded-xl p-6 space-y-4'>{error&&<div className='bg-error/10 border border-error/30 rounded-bet p-3 text-error text-sm'>{error}</div>}<div><label className='text-sm text-text-secondary mb-1 block'>Email or Username</label><div className='relative'><Mail size={16} className='absolute left-3 top-1/2 -translate-y-1/2 text-text-muted'/><input type='text' value={email} onChange={e=>setEmail(e.target.value)} className='bet-input pl-10' placeholder='you@example.com' required/></div></div><div><label className='text-sm text-text-secondary mb-1 block'>Password</label><div className='relative'><Lock size={16} className='absolute left-3 top-1/2 -translate-y-1/2 text-text-muted'/><input type={show?'text':'password'} value={password} onChange={e=>setPassword(e.target.value)} className='bet-input pl-10 pr-10' placeholder='••••••••' required/><button type='button' onClick={()=>setShow(!show)} className='absolute right-3 top-1/2 -translate-y-1/2 text-text-muted'>{show?<EyeOff size={16}/>:<Eye size={16}/>}</button></div></div><button type='submit' disabled={loading} className='bet-button w-full py-3'>{loading?'Signing in...':'Sign In'}</button><p className='text-center text-sm text-text-muted'>No account? <Link href='/register' className='text-primary hover:underline font-semibold'>Create Account</Link></p></form></div></div>;
}