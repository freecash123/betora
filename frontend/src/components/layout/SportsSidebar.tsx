'use client';
import React from 'react';import Link from 'next/link';import { usePathname } from 'next/navigation';import { SPORTS } from '@/lib/constants';

export function SportsSidebar() {
  const pathname = usePathname();
  return (<aside className='hidden lg:block w-56 flex-shrink-0 border-r border-surface-border pt-20 pb-6 bg-surface'><nav className='sticky top-20 px-3 space-y-1'><div className='px-3 py-2 text-xs font-semibold text-text-muted uppercase tracking-wider'>All Sports</div><Link href='/live' className={`flex items-center gap-3 px-3 py-2.5 rounded-bet text-sm transition-all ${pathname==='/live'?'bg-primary/10 text-primary font-semibold':'text-text-secondary hover:text-white hover:bg-surface-lighter'}`}><span className='w-2 h-2 bg-error rounded-full live-pulse'/>{' '}<span className='font-semibold text-error'>Live Now</span></Link>{SPORTS.map(s=>{const active=pathname.includes(`/sports/${s.slug}`);return(<Link key={s.id} href={`/sports/${s.slug}`} className={`flex items-center gap-2 px-3 py-2.5 rounded-bet text-sm transition-all ${active?'bg-primary/10 text-primary font-semibold':'text-text-secondary hover:text-white hover:bg-surface-lighter'}`}><span className='text-lg'>{s.icon}</span><span>{s.name}</span></Link>)})}<div className='pt-4 mt-4 border-t border-surface-border'><Link href='/promotions' className='flex items-center gap-2 px-3 py-2.5 rounded-bet text-sm text-accent font-semibold hover:bg-surface-lighter transition-all'><span>🎁</span><span>Promotions</span></Link><Link href='/help' className='flex items-center gap-2 px-3 py-2.5 rounded-bet text-sm text-text-secondary hover:text-white hover:bg-surface-lighter transition-all'><span>❓</span><span>Help Centre</span></Link></div></nav></aside>);
}